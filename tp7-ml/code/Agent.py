from random import randint,choices, random
import math
import pandas as pd
from symbol import expr_stmt
import treelib as t

class Agent:
    #sa
    def plularityValue(dataset : pd.DataFrame):
        outputValues = pd.unique(set[""]);
        common = outputValues[0]
        for value in outputValues:
            #contar la cantidad de veces que aparece el valor
            print()
        return mostCommon
    
    #Applies 
    def Importance(arg,dataset):
        print()
    #
    def maxArgument(dataset):
        print()
    #
    def difference(setA,setB):
        
        return setC
   
   
    """
    def decisionTreeLearning(self,examples : pd.DataFrame,attributes : pd.DataFrame.,parentExample : pd.DataFrame) -> t.Tree:
        if(len(examples.index) == 0):
            return plularityValue(parentExample)
        elif(len(examples.index) == 0):
            return True
        elif (len(attributes) == 0 and) :
            return
        else:
            a = maxArgument(attributes)
            A = Importance(a,examples)
            tree = newTree(A)
            for vk in A:
                newExamples =  examples
                subtree = decisionTreeLearning(newExamples,difference(attributes,A),examples)
        return tree
    """